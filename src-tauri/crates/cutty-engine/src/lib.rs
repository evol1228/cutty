//! # cutty-engine
//!
//! Owns all project and timeline state: the project model, timeline
//! operations, the command system (apply/invert), and undo/redo.
//!
//! Phase 0: intentionally empty skeleton. Editor features (timeline model,
//! commands) land in Phase 1 — see PLAN.md §5.

#[cfg(test)]
mod tests {
    #[test]
    fn skeleton_compiles() {}
}
